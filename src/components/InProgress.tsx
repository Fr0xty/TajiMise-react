import '../styles/InProgress.scss';

const InProgress = () => {
    return (
        <div className="in-progress">
            <h1>Coming Soon</h1>
        </div>
    );
};

export default InProgress;
