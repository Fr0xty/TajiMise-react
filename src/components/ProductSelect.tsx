import '../styles/ProductSelect.scss';

const ProductSelect = () => {
    return (
        <div className="product-select">
            <p>No product as of now.</p>
        </div>
    );
};

export default ProductSelect;
